# Normalise text files to LF in the repository
* text=auto

# Windows scripts keep CRLF when checked out
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# Binary files
*.png  binary
*.pdf  binary

# npx reel-agent runs on macOS/Linux too: a CRLF shebang would break it
npm/** text eol=lf
